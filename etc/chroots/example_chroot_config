# This is a minimal example configuration to use as a starting point.
#
# Defines which mounts should be used for the chroot. Space separated.
MOUNT_CONFIGS="base" 

# If you want to copy some files to your chroot, you can define sets
# like you can do for MOUNT_CONFIGS. Space separated.
FILE_CONFIGS="base"

# You can use devices, too.
#CHROOT_DEV="/dev/vg/amd64_stable"
